SERPAPI_API_KEY=your_serpapi_key
GROQ_API_KEY=optional_groq_key

# Use one persistent store in production so URLs are not repeated in later runs.
# Vercel KV / Upstash:
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Or Postgres / Neon / Vercel Postgres:
POSTGRES_URL=
DATABASE_URL=

